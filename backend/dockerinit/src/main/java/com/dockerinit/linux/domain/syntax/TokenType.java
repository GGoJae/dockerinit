package com.dockerinit.linux.domain.syntax;

public enum TokenType {
    COMMAND,
    OPTION,
    ARGUMENT,
    SOURCE,
    DESTINATION,
    PATH,
    FILE,
    DIRECTORY,
    FLAG,
    VALUE,
    LITERAL
}
